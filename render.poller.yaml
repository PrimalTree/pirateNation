services:
  - type: worker
    name: pirate-poller
    env: docker
    dockerfilePath: Dockerfile.poller
    plan: starter
    autoDeploy: true
    envVars:
      - key: SUPABASE_URL
        sync: True  
      - key: SUPABASE_SERVICE_ROLE
        sync: True
      - key: LIVE_SOURCE_URL
        sync: True
      - key: ADMIN_POLL_TOKEN
        sync: True
      - key: LIVE_POLL_INTERVAL_MS
        value: "30000"
