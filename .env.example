###############################################
# Pirate Nation – Environment Variables (MVP) #
###############################################
#
# Usage
# 1) Copy this file to `.env.local` at the repo root
# 2) Fill in the values below
# 3) Restart dev servers after changes
#
# Notes
# - Variables prefixed with NEXT_PUBLIC_ are safe for the browser
# - Never expose the Supabase service role key to the client
# - Both apps (web, admin) read from this root .env

#################################
# Branding / UI
#################################
# Display name used in the web header
NEXT_PUBLIC_SITE_NAME=Pirate Nation

#################################
# Map (Optional)
#################################
# Optional Mapbox access token for the interactive map
# Get it from: https://account.mapbox.com/ -> Access tokens
NEXT_PUBLIC_MAPBOX_TOKEN=

#################################
# Supabase (Required)
#################################
# Project URL and anon key (browser)
# Get both from: Supabase Dashboard -> Project Settings -> API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Project URL and anon key (server)
# Same values as above, but used by server components/actions
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE=

#################################
# Analytics (Optional)
#################################
# PostHog project API key (client-side analytics). Optional.
# Get it from: PostHog -> Project Settings -> Project API Key
POSTHOG_API_KEY=

# Plausible domain (server-side events). Optional.
# Example: piratenation.app
PLAUSIBLE_DOMAIN=piratenation.app

#################################
# Payments (Optional)
#################################
# Stripe publishable & secret keys for Checkout
# Get from: https://dashboard.stripe.com/apikeys
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=

#################################
# Weather (Optional)
#################################
# OpenWeatherMap API key (for pregame weather)
# https://home.openweathermap.org/api_keys
OPENWEATHER_API_KEY=

#################################
# CFBD (Optional)
#################################
# College Football Data API key
# Get it from: https://collegefootballdata.com/
CFBD_API_KEY=

#################################
# Web Push (Optional – later)
#################################
# VAPID keys for Web Push notifications
# Generate with: npx web-push generate-vapid-keys
# or via your push service dashboard
WEB_PUSH_VAPID_PUBLIC_KEY=
WEB_PUSH_VAPID_PRIVATE_KEY=

#################################
# Admin Poller (server-side only)
#################################
# Token forwarded to the upstream live endpoint as Authorization: Bearer
ADMIN_POLL_TOKEN=
# Optional override for source URL and interval
LIVE_SOURCE_URL=https://api.example.com/live
LIVE_POLL_INTERVAL_MS=30000

# Cron job shared secret (query param)
CRON_SECRET=

# Admin: full cron URL for manual trigger (same domain as deployed web app)
# Example: https://piratenation.app/api/cron/live
CRON_URL=
